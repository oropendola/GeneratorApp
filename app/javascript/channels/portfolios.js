// Codigo en coffe
// ready = undefined
//
// ready = ->
//  $('.sortable').sortable()
//  return
//
// // el ready de jquery indica que cuando la pagina ha cargado le pasamos la funcion
// // de la variable ready
// $(document).ready ready

var ready = undefined

ready = function() {
	
  $('.sortable').sortable()
  return

}

$(document).ready ready




