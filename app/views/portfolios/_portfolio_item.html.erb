<p><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></p>

<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body %></p>
<%= image_tag portfolio_item.main_image unless portfolio_item.main_image.nil? %>
<%= link_to "Editar portafolio", edit_portfolio_url(portfolio_item) %>
<%= link_to "Borrar", portfolio_path(portfolio_item), method: :delete, data: { confirm: "Esta seguro ?" } %>